


    ```mermaid
    sequenceDiagram
    participant 主持人
    participant 小程序
    participant API网关
    participant 会议服务
    participant 实时通信服务
    participant 参会者
    
    主持人->>小程序: 开始会议
    小程序->>API网关: POST /meeting/start
    API网关->>会议服务: 验证令牌
    会议服务->>会议服务: 创建会议记录
    会议服务->>实时通信服务: 广播会议开始
    实时通信服务->>参会者: 推送会议开始通知
    参会者->>参会者: 自动进入会议
    ```