 ```mermaid
   graph LR
    A[区域A] -->|专线| B[区域B]
    B -->|专线| C[区域C]
    
    subgraph 区域A
        D[可用区1] --> E[K8s节点]
        F[可用区2] --> G[K8s节点]
        H[可用区3] --> I[K8s节点]
    end
    
    subgraph 区域B
        J[MySQL主] --> K[MySQL从]
        K --> L[MySQL从]
    end
    
    subgraph 区域C
        M[Ceph存储集群] --> N[监控节点]
    end
```